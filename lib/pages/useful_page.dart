import 'package:flutter/material.dart';

class UsefulFunctionPage extends StatelessWidget {
  const UsefulFunctionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Useful Page')));
  }
}
